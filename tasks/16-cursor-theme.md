# Cursor Theme

Replace the default cursor with a simpler, cleaner icon set.

## Notes

- Set via `home.pointerCursor` in home-manager (handles GTK, X11, Hyprland)
- Popular minimal options: Bibata-Modern-Classic, Adwaita, Phinger
- Should set `size`, `name`, `package`, and `gtk.enable = true`
