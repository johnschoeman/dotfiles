# Hyprlock visual overhaul

## Priority: Low

## Status

Not started.

## Goal

Improve the hyprlock lock screen beyond the current minimal input field. Explore what hyprlock supports — layout, styling, clock/date display, background options, label text, fonts — and design a polished lock screen.

## Current State

Basic config in `hyprlock/hyprlock.conf`: screenshot background with blur, single input field at center-bottom. Colors already wired into theme-select.

## To Investigate

- Clock / date labels (position, font, size)
- Input field styling (rounding, border, placeholder text, size)
- Background options (wallpaper vs screenshot, blur tuning)
- Font selection
- Additional labels (greeting, user info)
- What other hyprlock widgets/features are available
