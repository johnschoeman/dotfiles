# <!-- Area Name -->

<!-- One sentence: what this area contains and its entry point. Example: "Data fetching infrastructure and backend integration. Entry point: `src/api/`." -->

## <!-- Section heading (e.g. Core Files, Components, Hooks) -->

| File | Purpose |
|------|---------|
| `<!-- path/to/file.ts -->` | <!-- what it does --> |

## <!-- Additional section if needed -->

| File | Purpose |
|------|---------|
| `<!-- path/to/file.ts -->` | <!-- what it does --> |

---

**Format rules:**

- Title + one-sentence intro that names the entry point directory
- File paths relative to project root, wrapped in backticks
- Purpose column: one short phrase, no trailing period
- Group related files under a section heading
- No file counts, no line counts, no implementation details
- Under 150 lines per file; if longer, split into sub-files
