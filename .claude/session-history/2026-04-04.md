# 2026-04-04

## Skill renames and Claude Code update channel

**Thread:** Rename verb-noun skills to noun-verb format for consistency, then set up a faster Claude Code update channel via a dedicated Nix flake.

Renamed 5 catalog skills (`wrap-session` → `session-wrap`, `wrap-day` → `today-wrap`, `update-knowledge` → `knowledge-update`, `update-session-log` → `session-update`, `content-synthesize` → `content-process`). Updated all internal references — catalog templates, project-local overrides in `.claude/skills/`, cross-references in CLAUDE.md, KNOWLEDGE.md, setup-claude, add-project-skill, and content-capture. Session history files left as-is (historical records). Committed the skill renames.

Then pivoted to package management. The `nupdate` fish alias only worked from the nixos directory — added `--flake ~/dotfiles/nixos` flag. Added `nupdated` alias to check last flake update time via `stat` on `flake.lock`.

User ran `nix flake update` and hit a 404 for `claude-code-2.1.88.tgz` — the npm tarball was delisted from the registry. The nixpkgs-packaged version was stuck at 2.1.39. Added the `sadjow/claude-code-nix` flake (hourly auto-updated) as a new flake input, passed the package to home-manager via `extraSpecialArgs`, and swapped it into `home.nix`.

**Open:** User hasn't rebuilt yet (`nrs`) — the claude-code-nix flake integration is untested. Fish alias changes also pending rebuild. Uncommitted changes: flake.nix, flake.lock, home.nix, fish.nix.
