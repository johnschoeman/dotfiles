# 2026-03-16

## Nix formatter behavior and autoformatting decision

**Thread:** Investigated whether to use nixfmt or nixpkgs-fmt for formatting .nix files, and whether to keep autoformatting enabled.

Started by adding Thunderbird as an Outlook alternative for NixOS, which led to discovering that nixfmt was collapsing vertically-aligned inline comments to single-space format. Researched both nixfmt and nixpkgs-fmt to see if either could preserve vertical comment alignment.

Created test files and ran both formatters. Found that both nixfmt and nixpkgs-fmt produce identical output — they both collapse vertical spacing to a single space before `#` comments. This is documented in nixfmt's standard as explicitly out of scope: "no special care is taken to preserve vertical alignment in the AST or comments."

Decided to disable autoformatting entirely to preserve manual formatting preferences. Removed the "After editing or creating `.nix` files: Run `nixfmt` on them before finishing" instruction from CLAUDE.md. The `nixfmt-rfc-style` package remains installed for optional manual use.

Also cleaned up `.claude/settings.local.json` permissions — removed colons from Bash permissions (e.g., `Bash(lsof:*)` → `Bash(lsof *)`), removed duplicate git status permissions, reorganized for readability, and moved `nh os switch` to the deny list per CLAUDE.md guidance.

**Open:** Staged changes ready to commit. Decided against using Thunderbird after testing.

## Enable hypridle for idle management

**Thread:** Enabled hypridle to handle screen lock and dpms on idle, replacing the old swayidle-based setup.

Changes made:
- Uncommented and updated `services.hypridle` in `nixos/home/hyprland.nix` — lock at 600s (10 min), dpms off at 900s (15 min), no suspend
- Removed `swayidle` package from `nixos/configuration.nix`
- Removed `exec-once = ~/dotfiles/hypr/suspend.sh` from `hypr/hyprland.conf` — hypridle is managed as a systemd service by home-manager
- Deleted `hypr/suspend.sh` (entirely commented out, superseded)
- Moved task `01-enable-hypridle.md` to `tasks/_done/`

**Decisions:**
- 600s/900s timeouts chosen (lock then dpms off 5 min later)
- No suspend listener — just lock and screen off
- hypridle via home-manager `services.hypridle` means it starts as a systemd user service, no exec-once needed

**Open:** Idle timeouts not yet tested in practice — will verify lock and dpms behavior over time.

## Add Nix garbage collection and boot entry limit

**Thread:** Prevent old NixOS generations from accumulating indefinitely and filling `/boot`.

Added two settings to `nixos/configuration.nix`: weekly automatic garbage collection (`nix.gc` with `--delete-older-than 14d`) and a boot entry cap (`configurationLimit = 15`). Clarified that the current running generation is always a GC root — it's never deleted regardless of age, so the 14d window is safe even with long gaps between rebuilds.

Task `02-nix-garbage-collection.md` moved to `tasks/_done/`.

**Open:** Changes uncommitted. Needs `nrs` to apply — verify with `systemctl list-timers | grep nix-gc` after rebuild.

## Remove unused and duplicate packages

**Thread:** Clean up package clutter — remove kitty, wl-clipboard-rs, and consolidate yazi.

Kitty was still enabled via `programs.kitty.enable = true` in hyprland.nix despite Alacritty being the actual terminal. `wl-clipboard-rs` in configuration.nix duplicated the already-present `wl-clipboard`. And yazi was listed as a raw package in home.nix while a dedicated `yazi.nix` module existed but was commented out.

Removed kitty, removed wl-clipboard-rs, uncommented the yazi.nix import, and removed the duplicate yazi from home.packages. Task `03-remove-unused-packages.md` moved to `tasks/_done/`.

**Open:** Changes uncommitted. Needs `nrs` to apply — verify kitty is gone, wl-copy still works, yazi still works.
